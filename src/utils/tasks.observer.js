import { observerFactory } from "./observer";

export const createTaskObserver = observerFactory(null)
export const removeTaskObserver = observerFactory(null)
export const selectTaskObserver = observerFactory(null)